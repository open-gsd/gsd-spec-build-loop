interface:
  display_name: "GSD Loop Schedule"
  short_description: "Keep builder and reviewer passes running"
  default_prompt: "Use $gsd-loop-schedule to keep the GSD build and review loops running."
